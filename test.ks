module Test

struct Person {
    var name: String
    var age: Int

    func greet() -> String {
        return "Hello, " + self.name
    }
}

func main() {
    let p = Person(name: "Alice", age: 30);
    // Try typing p. here to see completions
    p.
}
