module Test

func main() -> lang.i64 {
    var arr = std.collections.Array[std.num.Int64]();
    arr.append(1);
    arr.append(2);

    // Test map with explicit closure parameter
    let doubled = arr.iter().map({ (x) in x * 2 }).collect();

    0
}
