/// Format-agnostic request body parsing via Quill.
///
/// Format-based body parsing (`parseBody`, `parseBodyAs`) requires
/// metatype support (`F.Type`) which is not yet available. These
/// functions will be added when the compiler supports metatypes.

module perch.body
