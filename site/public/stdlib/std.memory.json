{
  "path": "std.memory",
  "name": "memory",
  "submodules": [],
  "items": [
    {
      "kind": "protocol",
      "name": "Allocator",
      "anchor": "protocol-Allocator",
      "signature": "public protocol Allocator",
      "doc": "Protocol for raw-memory allocators.\n\n`Allocator` is the indirection collections use so they can be parameterised\nover allocation strategy (e.g. `Array[T, A]`, `Buffer[T, A]`, custom\narenas). All three methods are `mutating` so stateful allocators (arenas,\npools) can update their bookkeeping; stateless wrappers around `malloc`\ndon't need to.\n\n# Examples\n\n```\nvar alloc = SystemAllocator();\nif let .Some(p) = alloc.allocate(Layout.of[Int64]()) {\n    // ... use p ...\n    alloc.deallocate(p, Layout.of[Int64]())\n}\n```",
      "source_path": "lang/std/memory/allocator.ks",
      "member_groups": [
        {
          "kind": "direct",
          "label": null,
          "source_path": null,
          "members": [
            {
              "kind": "function",
              "name": "allocate",
              "anchor": "function-allocate",
              "signature": "mutating func allocate(Layout) -> RawPointer?",
              "doc": "Returns a pointer to a fresh region matching `layout`, or `.None`\nwhen allocation fails. Returned memory is uninitialised.",
              "source_path": "lang/std/memory/allocator.ks"
            },
            {
              "kind": "function",
              "name": "deallocate",
              "anchor": "function-deallocate",
              "signature": "mutating func deallocate(RawPointer, Layout)",
              "doc": "Releases memory previously returned by `allocate` / `reallocate`.\n`layout` must match the layout used to obtain the pointer.\n\n# Safety\n\n`ptr` must have been produced by this allocator (or a clone of it)\nfor `layout`. Mismatching the layout, double-freeing, or freeing a\npointer from another allocator is undefined behavior.",
              "source_path": "lang/std/memory/allocator.ks"
            },
            {
              "kind": "function",
              "name": "reallocate",
              "anchor": "function-reallocate",
              "signature": "mutating func reallocate(RawPointer, Layout, Layout) -> RawPointer?",
              "doc": "Resizes the allocation at `ptr` from `oldLayout` to `newLayout`.\nOn failure the original allocation is left intact and `.None` is\nreturned. On success the old pointer must not be reused — use the\nreturned pointer instead.",
              "source_path": "lang/std/memory/allocator.ks"
            }
          ]
        }
      ]
    },
    {
      "kind": "struct",
      "name": "Buffer",
      "anchor": "struct-Buffer",
      "signature": "public struct Buffer[T, A] where A: Allocator { /* private fields */ }",
      "doc": "Owning, allocator-parameterised contiguous storage.\n\n`Buffer` is the building block underneath `Array`, `String`, and any\nother COW/growable collection. It owns its allocation, deallocates on\ndrop, and is `not Copyable` to keep ownership unique. For a non-owning\nview see `Slice`; for a refcounted owning wrapper see `RcBox`.\n\n# Examples\n\n```\nvar buf = Buffer[Int64, SystemAllocator](capacity: 4, allocator: SystemAllocator());\nbuf.write(at: 0, value: 10);\nbuf.write(at: 1, value: 20);\nbuf.read(at: 0)              // .Some(10)\nbuf.resize(to: 8);           // grow in place if possible\n```\n\n# Representation\n\nA `Pointer[T]` to the storage, an `Int64` capacity, and the allocator\ninstance. The buffer's contents are not initialised on construction —\nreading an uninitialised slot is undefined behavior.\n\n# Memory Model\n\nOwning, unique. The deinit reclaims storage via the same allocator.\nMarked `not Copyable` so an accidental `let b2 = b1` is rejected at\ncompile time; use a higher-level COW wrapper (e.g. via `RcBox`) for\nshared semantics.",
      "source_path": "lang/std/memory/buffer.ks",
      "member_groups": [
        {
          "kind": "direct",
          "label": null,
          "source_path": null,
          "members": [
            {
              "kind": "initializer",
              "name": "With Capacity",
              "anchor": "initializer-With-Capacity",
              "signature": "public init(Int64, A)",
              "doc": "Allocates a buffer holding `capacity` elements. Storage is\nuninitialised; the caller is responsible for writing valid `T`s\nbefore reading them.\n\n# Errors\n\nPanics with `\"Buffer allocation failed\"` if `allocator.allocate`\nreturns `.None`.",
              "source_path": "lang/std/memory/buffer.ks"
            },
            {
              "kind": "function",
              "name": "asSlice",
              "anchor": "function-asSlice",
              "signature": "public func asSlice() -> Slice[T]",
              "doc": "Returns a `Slice[T]` over the entire buffer. The slice does not\nextend the buffer's lifetime; callers must keep the buffer alive\nfor as long as they use the slice.",
              "source_path": "lang/std/memory/buffer.ks"
            },
            {
              "kind": "field",
              "name": "capacity",
              "anchor": "field-capacity",
              "signature": "public var capacity: Int64 { get }",
              "doc": "Number of element slots — not the count of *initialised* elements.",
              "source_path": "lang/std/memory/buffer.ks"
            },
            {
              "kind": "field",
              "name": "pointer",
              "anchor": "field-pointer",
              "signature": "public var pointer: Pointer[T] { get }",
              "doc": "Pointer to the first slot.",
              "source_path": "lang/std/memory/buffer.ks"
            },
            {
              "kind": "function",
              "name": "read",
              "anchor": "function-read",
              "signature": "public func read(unchecked: Int64) -> T",
              "doc": "Reads slot `index` without bounds checking.\n\n# Safety\n\n`index` must satisfy `0 <= index < capacity`, and the slot must\nalready hold an initialised `T`. Out-of-range or uninitialised\nreads are undefined behavior.",
              "source_path": "lang/std/memory/buffer.ks"
            },
            {
              "kind": "function",
              "name": "read",
              "anchor": "function-read",
              "signature": "public func read(at: Int64) -> T?",
              "doc": "Reads slot `index`, returning `.None` when out of range. As with\nthe unchecked form, the slot must already hold an initialised `T`.",
              "source_path": "lang/std/memory/buffer.ks"
            },
            {
              "kind": "function",
              "name": "resize",
              "anchor": "function-resize",
              "signature": "public mutating func resize(to: Int64)",
              "doc": "Grows or shrinks the storage to hold `newCapacity` elements via\nthe allocator's `reallocate`. On success, existing initialised\nelements are preserved up to the smaller of the two capacities;\nthe new pointer becomes the buffer's storage.\n\n# Errors\n\nPanics with `\"Buffer resize failed\"` if `reallocate` returns\n`.None` (the original allocation is left intact, but the panic\naborts).",
              "source_path": "lang/std/memory/buffer.ks"
            },
            {
              "kind": "function",
              "name": "slice",
              "anchor": "function-slice",
              "signature": "public func slice(from: Int64, to: Int64) -> Slice[T]?",
              "doc": "Returns a slice over `[start, end)`, or `.None` when the range\nfalls outside `[0, capacity]`. As with `asSlice`, the slice\nborrows from the buffer.",
              "source_path": "lang/std/memory/buffer.ks"
            },
            {
              "kind": "function",
              "name": "write",
              "anchor": "function-write",
              "signature": "public func write(unchecked: Int64, T)",
              "doc": "Writes `value` into slot `index` without bounds checking.\n\n# Safety\n\nSame precondition as `read(unchecked:)` — `0 <= index < capacity`.",
              "source_path": "lang/std/memory/buffer.ks"
            },
            {
              "kind": "function",
              "name": "write",
              "anchor": "function-write",
              "signature": "public func write(at: Int64, T) -> Bool",
              "doc": "Writes `value` to slot `index`. Returns `false` (and does\nnothing) when out of range.",
              "source_path": "lang/std/memory/buffer.ks"
            }
          ]
        }
      ]
    },
    {
      "kind": "typealias",
      "name": "GlobalAllocator",
      "anchor": "typealias-GlobalAllocator",
      "signature": "public type GlobalAllocator = SystemAllocator",
      "doc": "Project-wide default allocator, aliased to `SystemAllocator`. Override\nat the project level if a global custom allocator is needed.",
      "source_path": "lang/std/memory/allocator.ks"
    },
    {
      "kind": "struct",
      "name": "Layout",
      "anchor": "struct-Layout",
      "signature": "public struct Layout { /* private fields */ }",
      "doc": "Size + alignment pair describing the memory footprint of a type.\n\nAllocators take a `Layout` rather than a raw byte count so they can\nhonour alignment requirements (SIMD types, page-aligned buffers, etc.).\nThe static `of[T]` and `array[T]` factories cover the common cases;\n`merge` and `padToAlign` exist for hand-rolled struct layouts.\n\n# Examples\n\n```\nlet l = Layout.of[Int64]();           // size 8, alignment 8\nlet buf = Layout.array[UInt8](1024);  // size 1024, alignment 1\nallocator.allocate(l)\n```\n\n# Representation\n\nTwo `Int64`s — `size` and `alignment`. No invariants enforced at\nconstruction; misaligned layouts are caught (or undefined) at the\nallocator level.",
      "source_path": "lang/std/memory/layout.ks",
      "member_groups": [
        {
          "kind": "direct",
          "label": null,
          "source_path": null,
          "members": [
            {
              "kind": "initializer",
              "name": "From Fields",
              "anchor": "initializer-From-Fields",
              "signature": "public init(size: Int64, alignment: Int64)",
              "doc": "Builds a layout from explicit `size` and `alignment`. Caller is\nresponsible for keeping `alignment` a power of two.",
              "source_path": "lang/std/memory/layout.ks"
            },
            {
              "kind": "field",
              "name": "alignment",
              "anchor": "field-alignment",
              "signature": "public var alignment: Int64",
              "doc": "Required alignment in bytes — always a power of two for layouts\nproduced by `of`/`array`.",
              "source_path": "lang/std/memory/layout.ks"
            },
            {
              "kind": "function",
              "name": "array",
              "anchor": "function-array",
              "signature": "public static func array[T](Int64) -> Layout",
              "doc": "Layout for `count` contiguous `T` values. Inherits the element's\nalignment; size is `sizeof[T] * count` with no inter-element padding\n(T is assumed already padded to its own alignment).",
              "source_path": "lang/std/memory/layout.ks"
            },
            {
              "kind": "function",
              "name": "merge",
              "anchor": "function-merge",
              "signature": "public func merge(with: Layout) -> (Layout, Int64)",
              "doc": "Concatenates `other` after `self`, mimicking how a C struct lays\nout its second field. Returns the combined layout and the byte\noffset where `other`'s storage starts (handy for building field\naccess tables by hand).",
              "source_path": "lang/std/memory/layout.ks"
            },
            {
              "kind": "function",
              "name": "of",
              "anchor": "function-of",
              "signature": "public static func of[T]() -> Layout",
              "doc": "Layout for a single value of `T` — uses the compiler-known\n`sizeof` and `alignof` for the type.",
              "source_path": "lang/std/memory/layout.ks"
            },
            {
              "kind": "function",
              "name": "padToAlign",
              "anchor": "function-padToAlign",
              "signature": "public func padToAlign() -> Layout",
              "doc": "Rounds `size` up to the next multiple of `alignment`. Use when\nemitting a value into a packed array — without padding, element\n`i+1` would land at the wrong offset.",
              "source_path": "lang/std/memory/layout.ks"
            },
            {
              "kind": "field",
              "name": "size",
              "anchor": "field-size",
              "signature": "public var size: Int64",
              "doc": "Footprint in bytes.",
              "source_path": "lang/std/memory/layout.ks"
            }
          ]
        },
        {
          "kind": "protocol",
          "label": "Equatable",
          "source_path": "std.core.Equatable",
          "members": [
            {
              "kind": "function",
              "name": "equals",
              "anchor": "function-equals",
              "signature": "public func equals(Layout) -> Bool",
              "doc": "Equal when both fields match.",
              "source_path": "lang/std/memory/layout.ks"
            }
          ]
        }
      ]
    },
    {
      "kind": "struct",
      "name": "LiteralSlice",
      "anchor": "struct-LiteralSlice",
      "signature": "public struct LiteralSlice[T] { /* private fields */ }",
      "doc": "Read-only view over the compiler-emitted backing buffer for an array\nliteral.\n\nUser code rarely names this type directly: it appears in\n`ExpressibleByArrayLiteral.init(arrayLiteral:)` and friends so that\ntypes accepting `[a, b, c]` literals can iterate the elements without\ntouching raw pointers. The slice does **not** own the storage — the\ncompiler keeps the literal alive for the duration of the call.\n\n# Examples\n\n```\n// Conforming to ExpressibleByArrayLiteral\npublic struct MyVec[T]: ExpressibleByArrayLiteral {\n    type Element = T\n    public init(arrayLiteral lit: LiteralSlice[T]) {\n        var v = MyVec();\n        for x in lit { v.push(x) }\n        self = v\n    }\n}\n```\n\n# Memory Model\n\nNon-owning. The backing storage is compiler-managed and lives for the\nscope of the literal expression. Capturing a `LiteralSlice` past that\nscope is a use-after-free.",
      "source_path": "lang/std/memory/literal_slice.ks",
      "member_groups": [
        {
          "kind": "direct",
          "label": null,
          "source_path": null,
          "members": [
            {
              "kind": "subscript",
              "name": "Checked Index",
              "anchor": "subscript-Checked-Index",
              "signature": "public subscript(checked: Int64) -> T? { get }",
              "doc": "Reads element `index`, returning `.None` on out-of-bounds.",
              "source_path": "lang/std/memory/literal_slice.ks"
            },
            {
              "kind": "initializer",
              "name": "From Storage",
              "anchor": "initializer-From-Storage",
              "signature": "public init(pointer: lang.ptr[T], count: lang.i64)",
              "doc": "Builds the slice from the raw pointer and count the compiler emits.",
              "source_path": "lang/std/memory/literal_slice.ks"
            },
            {
              "kind": "subscript",
              "name": "Indexed",
              "anchor": "subscript-Indexed",
              "signature": "public subscript(Int64) -> T { get }",
              "doc": "Reads element `index`, panicking on out-of-bounds.\n\nThe default subscript: trades a single comparison for a guaranteed\ntrap on bad input. Use `(unchecked:)` inside compiler-emitted init\npaths where the index is statically known in range, or\n`(checked:)` to handle out-of-range without a panic.\n\n# Errors\n\nPanics with `\"LiteralSlice index out of bounds\"` if `index < 0`\nor `index >= count`.",
              "source_path": "lang/std/memory/literal_slice.ks"
            },
            {
              "kind": "subscript",
              "name": "Unchecked Index",
              "anchor": "subscript-Unchecked-Index",
              "signature": "public subscript(unchecked: Int64) -> T { get }",
              "doc": "Reads element `index` without bounds checking.\n\n# Safety\n\nUndefined behavior if `index < 0` or `index >= count`. Compiler-\nemitted init paths that use this guarantee the index is in range;\ndo not expose this subscript to user input without checking\n`count` first.",
              "source_path": "lang/std/memory/literal_slice.ks"
            },
            {
              "kind": "field",
              "name": "count",
              "anchor": "field-count",
              "signature": "public var count: Int64 { get }",
              "doc": "Number of elements in the literal.",
              "source_path": "lang/std/memory/literal_slice.ks"
            },
            {
              "kind": "field",
              "name": "isEmpty",
              "anchor": "field-isEmpty",
              "signature": "public var isEmpty: Bool { get }",
              "doc": "`true` for `[]`.",
              "source_path": "lang/std/memory/literal_slice.ks"
            }
          ]
        },
        {
          "kind": "protocol",
          "label": "Iterable",
          "source_path": "std.iter.Iterable",
          "members": [
            {
              "kind": "typealias",
              "name": "Item",
              "anchor": "typealias-Item",
              "signature": "type Item = T",
              "doc": "",
              "source_path": "lang/std/memory/literal_slice.ks"
            },
            {
              "kind": "typealias",
              "name": "TargetIterator",
              "anchor": "typealias-TargetIterator",
              "signature": "type TargetIterator = LiteralSliceIterator[T]",
              "doc": "",
              "source_path": "lang/std/memory/literal_slice.ks"
            },
            {
              "kind": "function",
              "name": "iter",
              "anchor": "function-iter",
              "signature": "public func iter() -> LiteralSliceIterator[T]",
              "doc": "Iterator over the elements in source order.",
              "source_path": "lang/std/memory/literal_slice.ks"
            }
          ]
        }
      ]
    },
    {
      "kind": "struct",
      "name": "LiteralSliceIterator",
      "anchor": "struct-LiteralSliceIterator",
      "signature": "public struct LiteralSliceIterator[T] { /* private fields */ }",
      "doc": "Iterator yielded by `LiteralSlice.iter()`. Walks the backing buffer\nelement-by-element, advancing a typed pointer.\n\n# Representation\n\nA `Pointer[T]` plus a remaining count. No `Slice` indirection — the\niterator is what `LiteralSlice` hands out instead of exposing its\npointer directly.",
      "source_path": "lang/std/memory/literal_slice.ks",
      "member_groups": [
        {
          "kind": "direct",
          "label": null,
          "source_path": null,
          "members": [
            {
              "kind": "initializer",
              "name": "From Storage",
              "anchor": "initializer-From-Storage",
              "signature": "public init(ptr: Pointer[T], remaining: Int64)",
              "doc": "Builds an iterator from a typed pointer and element count.\nNot normally called by user code.",
              "source_path": "lang/std/memory/literal_slice.ks"
            }
          ]
        },
        {
          "kind": "protocol",
          "label": "Iterator",
          "source_path": "std.iter.Iterator",
          "members": [
            {
              "kind": "typealias",
              "name": "Item",
              "anchor": "typealias-Item",
              "signature": "type Item = T",
              "doc": "",
              "source_path": "lang/std/memory/literal_slice.ks"
            },
            {
              "kind": "function",
              "name": "next",
              "anchor": "function-next",
              "signature": "public mutating func next() -> T?",
              "doc": "Yields the next element, or `.None` once the buffer is exhausted.",
              "source_path": "lang/std/memory/literal_slice.ks"
            }
          ]
        }
      ]
    },
    {
      "kind": "struct",
      "name": "Pointer",
      "anchor": "struct-Pointer",
      "signature": "public struct Pointer[T] { /* private fields */ }",
      "doc": "Typed pointer to a single value of `T`.\n\nElement-typed counterpart to `RawPointer`: `offset(by:)` strides in\nunits of `sizeof[T]`, and `pointee` reads/writes through the address.\n`Pointer[T]` is FFI-safe when `T` is.\n\n# Examples\n\n```\nvar x = 42;\nlet p = Pointer(to: x);\np.read()                       // 42\np.write(100)                   // x is now 100\np.pointee = 7                  // x is now 7\n```\n\n# Representation\n\nOne `lang.ptr[T]`. The wrapping struct is purely a typing convenience —\nit lowers to a bare machine pointer.\n\n# Memory Model\n\nNon-owning. The pointee's lifetime is the caller's responsibility; the\npointer does not increment any refcount, register with any GC, or\ntrigger a deinit.",
      "source_path": "lang/std/memory/pointer.ks",
      "member_groups": [
        {
          "kind": "direct",
          "label": null,
          "source_path": null,
          "members": [
            {
              "kind": "initializer",
              "name": "From Raw",
              "anchor": "initializer-From-Raw",
              "signature": "public init(raw: lang.ptr[T])",
              "doc": "Wraps an existing primitive pointer.",
              "source_path": "lang/std/memory/pointer.ks"
            },
            {
              "kind": "initializer",
              "name": "To Value",
              "anchor": "initializer-To-Value",
              "signature": "public init(to: T)",
              "doc": "Takes the address of `value`. Equivalent to `&value` in C — the\ncaller must ensure `value` outlives any use of the resulting\npointer.",
              "source_path": "lang/std/memory/pointer.ks"
            },
            {
              "kind": "field",
              "name": "address",
              "anchor": "field-address",
              "signature": "public var address: UInt64 { get }",
              "doc": "Numeric address — same value as `asRaw().address`.",
              "source_path": "lang/std/memory/pointer.ks"
            },
            {
              "kind": "function",
              "name": "asRaw",
              "anchor": "function-asRaw",
              "signature": "public func asRaw() -> RawPointer",
              "doc": "Drops the type tag, returning a `RawPointer` to the same address.",
              "source_path": "lang/std/memory/pointer.ks"
            },
            {
              "kind": "function",
              "name": "cast",
              "anchor": "function-cast",
              "signature": "public func cast[U]() -> Pointer[U]",
              "doc": "Reinterprets the address as a `Pointer[U]`.\n\n# Safety\n\nSame caveats as `RawPointer.cast` — the storage must be valid for\n`U` (size, alignment, contents) at the moment of the read/write.",
              "source_path": "lang/std/memory/pointer.ks"
            },
            {
              "kind": "field",
              "name": "isNull",
              "anchor": "field-isNull",
              "signature": "public var isNull: Bool { get }",
              "doc": "Convenience for `address == 0`.",
              "source_path": "lang/std/memory/pointer.ks"
            },
            {
              "kind": "function",
              "name": "nullPointer",
              "anchor": "function-nullPointer",
              "signature": "public static func nullPointer() -> Pointer[T]",
              "doc": "Returns a typed null pointer.",
              "source_path": "lang/std/memory/pointer.ks"
            },
            {
              "kind": "function",
              "name": "offset",
              "anchor": "function-offset",
              "signature": "public func offset(by: Int64) -> Pointer[T]",
              "doc": "Strides the pointer by `n` *elements* (multiplied by `sizeof[T]`).\nCompare with `RawPointer.offset`, which strides by raw bytes.",
              "source_path": "lang/std/memory/pointer.ks"
            },
            {
              "kind": "field",
              "name": "pointee",
              "anchor": "field-pointee",
              "signature": "public var pointee: T { get set }",
              "doc": "Live view of the value at the address. `get` reads through the\npointer; `set` writes. Both are unchecked — see `# Safety`.\n\n# Safety\n\nThe pointer must be non-null and the storage must hold a valid\ninitialised `T`. Reading past the end of an allocation, after\nthe pointee has been freed, or through a dangling pointer is\nundefined behavior.",
              "source_path": "lang/std/memory/pointer.ks"
            },
            {
              "kind": "field",
              "name": "raw",
              "anchor": "field-raw",
              "signature": "public var raw: lang.ptr[T] { get }",
              "doc": "The wrapped primitive pointer.",
              "source_path": "lang/std/memory/pointer.ks"
            },
            {
              "kind": "function",
              "name": "read",
              "anchor": "function-read",
              "signature": "public func read() -> T",
              "doc": "Reads `T` from the address. Same safety preconditions as `pointee.get`.",
              "source_path": "lang/std/memory/pointer.ks"
            },
            {
              "kind": "function",
              "name": "write",
              "anchor": "function-write",
              "signature": "public func write(T)",
              "doc": "Writes `value` through the pointer. Same safety preconditions as\n`pointee.set`.",
              "source_path": "lang/std/memory/pointer.ks"
            }
          ]
        },
        {
          "kind": "protocol",
          "label": "Equatable",
          "source_path": "std.core.Equatable",
          "members": [
            {
              "kind": "function",
              "name": "equals",
              "anchor": "function-equals",
              "signature": "public func equals(Pointer[T]) -> Bool",
              "doc": "Address-based equality.",
              "source_path": "lang/std/memory/pointer.ks"
            }
          ]
        },
        {
          "kind": "protocol",
          "label": "Hash",
          "source_path": "std.core.Hash",
          "members": [
            {
              "kind": "function",
              "name": "hash",
              "anchor": "function-hash",
              "signature": "public func hash[H](into: mutating H) where H: Hasher",
              "doc": "Hashes the underlying address.\n\nHeap allocations cluster on alignment boundaries, so the raw\naddress has predictable low bits. We run the address through\nMurmur3's `fmix64` finalizer (two rounds of `xor-shift /\nmultiply`) before hashing so every input bit avalanches across\nthe 64-bit output. Without this, pointer-keyed maps see\ncollision clustering driven by the allocator's stride.",
              "source_path": "lang/std/memory/pointer.ks"
            }
          ]
        }
      ]
    },
    {
      "kind": "struct",
      "name": "RawPointer",
      "anchor": "struct-RawPointer",
      "signature": "public struct RawPointer { /* private fields */ }",
      "doc": "Untyped pointer to raw memory — `void*` in C terms.\n\nUsed at FFI boundaries and as an intermediate when casting between\ntyped pointers. `RawPointer` deliberately exposes no read/write methods\nof its own; cast to `Pointer[T]` first via `cast[T]()`. Equality and\nhashing are address-based.\n\n# Examples\n\n```\nlet p = RawPointer.nilPointer();\np.isNull                                // true\nlet typed: Pointer[Int64] = p.cast[Int64]()\n```\n\n# Representation\n\nOne `lang.ptr[lang.i8]`. FFI-safe — passes as a single machine pointer.",
      "source_path": "lang/std/memory/pointer.ks",
      "member_groups": [
        {
          "kind": "direct",
          "label": null,
          "source_path": null,
          "members": [
            {
              "kind": "initializer",
              "name": "From Address",
              "anchor": "initializer-From-Address",
              "signature": "public init(address: UInt64)",
              "doc": "Reconstructs a pointer from a numeric address. Useful for\nplatform-specific encodings (handles, MMIO addresses); incorrect\naddresses produce a pointer that dereferences to undefined memory.",
              "source_path": "lang/std/memory/pointer.ks"
            },
            {
              "kind": "initializer",
              "name": "From Raw",
              "anchor": "initializer-From-Raw",
              "signature": "public init(raw: lang.ptr[lang.i8])",
              "doc": "Wraps an existing primitive pointer.",
              "source_path": "lang/std/memory/pointer.ks"
            },
            {
              "kind": "field",
              "name": "address",
              "anchor": "field-address",
              "signature": "public var address: UInt64 { get }",
              "doc": "Numeric address of the pointee. Round-trips through\n`RawPointer(address:)`.",
              "source_path": "lang/std/memory/pointer.ks"
            },
            {
              "kind": "function",
              "name": "cast",
              "anchor": "function-cast",
              "signature": "public func cast[T]() -> Pointer[T]",
              "doc": "Reinterprets the address as a `Pointer[T]`.\n\n# Safety\n\nThe caller must ensure the address holds a valid `T` (correct size,\nalignment, and initialised contents) before reading through the\nreturned pointer.",
              "source_path": "lang/std/memory/pointer.ks"
            },
            {
              "kind": "field",
              "name": "isNull",
              "anchor": "field-isNull",
              "signature": "public var isNull: Bool { get }",
              "doc": "Convenience for `address == 0`.",
              "source_path": "lang/std/memory/pointer.ks"
            },
            {
              "kind": "function",
              "name": "nilPointer",
              "anchor": "function-nilPointer",
              "signature": "public static func nilPointer() -> RawPointer",
              "doc": "Returns the canonical null pointer.",
              "source_path": "lang/std/memory/pointer.ks"
            },
            {
              "kind": "function",
              "name": "offset",
              "anchor": "function-offset",
              "signature": "public func offset(by: Int64) -> RawPointer",
              "doc": "Adds `bytes` to the address (no element-size scaling — this is\nraw byte arithmetic). Use `Pointer[T].offset` for element-typed\nstrides.",
              "source_path": "lang/std/memory/pointer.ks"
            },
            {
              "kind": "field",
              "name": "raw",
              "anchor": "field-raw",
              "signature": "public var raw: lang.ptr[lang.i8]",
              "doc": "The wrapped primitive `i8*`.",
              "source_path": "lang/std/memory/pointer.ks"
            }
          ]
        },
        {
          "kind": "protocol",
          "label": "Equatable",
          "source_path": "std.core.Equatable",
          "members": [
            {
              "kind": "function",
              "name": "equals",
              "anchor": "function-equals",
              "signature": "public func equals(RawPointer) -> Bool",
              "doc": "Address-based equality. Two `RawPointer`s pointing into different\nallocations are equal iff their addresses coincide.",
              "source_path": "lang/std/memory/pointer.ks"
            }
          ]
        },
        {
          "kind": "protocol",
          "label": "Hash",
          "source_path": "std.core.Hash",
          "members": [
            {
              "kind": "function",
              "name": "hash",
              "anchor": "function-hash",
              "signature": "public func hash[H](into: mutating H) where H: Hasher",
              "doc": "Hashes the underlying address.\n\nHeap allocations cluster on alignment boundaries, so the raw\naddress has predictable low bits. We run the address through\nMurmur3's `fmix64` finalizer (two rounds of `xor-shift /\nmultiply`) before hashing so every input bit avalanches across\nthe 64-bit output. Without this, pointer-keyed maps see\ncollision clustering driven by the allocator's stride.",
              "source_path": "lang/std/memory/pointer.ks"
            }
          ]
        }
      ]
    },
    {
      "kind": "struct",
      "name": "RcBox",
      "anchor": "struct-RcBox",
      "signature": "public struct RcBox[T] { /* private fields */ }",
      "doc": "Heap allocation with a strong-reference count, used as the underlying\nstorage for the stdlib's copy-on-write types.\n\n`String`, `Array`, and `Dictionary` all wrap an `RcBox` so that a\nplain assignment shares storage and only the first mutating call pays\nfor a deep copy. Reach for `RcBox` directly when building a similar\nCOW type; for plain shared ownership without mutation prefer a more\npurpose-built container.\n\n# Examples\n\n```\nlet a = RcBox(value: [1, 2, 3]);\nlet b = a.clone();          // shares storage; refCount == 2\nif b.isUnique() { ... } else { let c = b.deepClone(); /* ... */ }\n```\n\n# Representation\n\nOne `Pointer[RcBoxStorage[T]]`. The pointed-to block holds an `Int64`\nrefcount followed by the `T` value, allocated via `SystemAllocator`.\n\n# Memory Model\n\nReference-counted, non-atomic (today — see TODOs). `clone()` increments\nthe count and shares storage; `deinit` decrements and frees on zero.\n`deepClone()` allocates a fresh `RcBox` carrying a copied value.\n\n# Guarantees\n\n- `isUnique()` returning `true` means in-place mutation is safe; this is\n  how COW types decide whether to copy.\n- The refcount is currently **not** atomic, so `RcBox` is not safe to\n  share across threads.",
      "source_path": "lang/std/memory/rcbox.ks",
      "member_groups": [
        {
          "kind": "direct",
          "label": null,
          "source_path": null,
          "members": [
            {
              "kind": "initializer",
              "name": "From Value",
              "anchor": "initializer-From-Value",
              "signature": "public init(T)",
              "doc": "Allocates fresh storage holding `value` with refcount 1. Panics if\nthe underlying `SystemAllocator` returns `.None`.\n\n# Errors\n\nPanics with `\"RcBox allocation failed\"` on allocation failure.",
              "source_path": "lang/std/memory/rcbox.ks"
            },
            {
              "kind": "function",
              "name": "clone",
              "anchor": "function-clone",
              "signature": "public func clone() -> RcBox[T]",
              "doc": "Bumps the refcount and returns a second `RcBox` pointing at the\nsame storage. The receiver and the returned box now both reference\nthe value; the next mutation should test `isUnique`.",
              "source_path": "lang/std/memory/rcbox.ks"
            },
            {
              "kind": "function",
              "name": "deepClone",
              "anchor": "function-deepClone",
              "signature": "public func deepClone() -> RcBox[T]",
              "doc": "Allocates fresh storage with a copy of the value. Used by COW\ntypes when `isUnique()` returns `false` — splits off a private\ncopy so the caller can mutate without affecting other clones.",
              "source_path": "lang/std/memory/rcbox.ks"
            },
            {
              "kind": "function",
              "name": "getValue",
              "anchor": "function-getValue",
              "signature": "public func getValue() -> T",
              "doc": "Reads the wrapped value out of storage. Returns a copy — the\nunderlying `T` is read through a pointer, so callers see a\nsnapshot, not a live reference.",
              "source_path": "lang/std/memory/rcbox.ks"
            },
            {
              "kind": "function",
              "name": "isUnique",
              "anchor": "function-isUnique",
              "signature": "public func isUnique() -> Bool",
              "doc": "Returns `true` when no other clone is sharing storage. The litmus\ntest for \"safe to mutate in place\" in COW collections.",
              "source_path": "lang/std/memory/rcbox.ks"
            },
            {
              "kind": "function",
              "name": "refCount",
              "anchor": "function-refCount",
              "signature": "public func refCount() -> Int64",
              "doc": "Current strong reference count. Mostly useful for tests and\ndiagnostics; production COW logic should branch on `isUnique`.",
              "source_path": "lang/std/memory/rcbox.ks"
            },
            {
              "kind": "function",
              "name": "setValue",
              "anchor": "function-setValue",
              "signature": "public func setValue(T)",
              "doc": "Overwrites the wrapped value in place. Safe only when this is the\nunique owner (`isUnique() == true`); otherwise other clones see the\nnew value, defeating COW. The COW types check `isUnique` before\ncalling this and `deepClone` otherwise.",
              "source_path": "lang/std/memory/rcbox.ks"
            }
          ]
        }
      ]
    },
    {
      "kind": "struct",
      "name": "Slice",
      "anchor": "struct-Slice",
      "signature": "public struct Slice[T] { /* private fields */ }",
      "doc": "Non-owning view over a contiguous run of `T` values.\n\n`Slice` is the standard \"borrow\" type for arrays, buffers, and any\nother contiguous storage: it stores a pointer + length and provides\nsafe and unchecked indexing, sub-slicing, iteration, and pattern\nmatching. The slice does **not** track or extend the lifetime of the\nunderlying storage — keeping a slice past the end of its source is a\nuse-after-free.\n\n# Examples\n\n```\nlet arr = [1, 2, 3, 4];\nlet s = arr.asSlice();\ns[safe: 0]                    // .Some(1)\ns[safe: 99]                   // .None\nfor x in s.iter() { print(x) }\n```\n\n# Memory Model\n\nNon-owning. Drop the source (`Array`, `Buffer`, literal scope) and the\nslice becomes dangling. Slices freely copy — they're just `(ptr, len)`\npairs.",
      "source_path": "lang/std/memory/pointer.ks",
      "member_groups": [
        {
          "kind": "direct",
          "label": null,
          "source_path": null,
          "members": [
            {
              "kind": "subscript",
              "name": "Checked Index",
              "anchor": "subscript-Checked-Index",
              "signature": "public subscript[I](checked: I) -> I.SliceYield? { get }",
              "doc": "Reads at `index`, returning `.None` on out-of-bounds.",
              "source_path": "lang/std/memory/pointer.ks"
            },
            {
              "kind": "subscript",
              "name": "Clamping",
              "anchor": "subscript-Clamping",
              "signature": "public subscript[I](clamped: I) -> I.SliceClampedYield { get set }",
              "doc": "Reads or writes at `index` with bounds saturated to `[0, count)`.\n`Int64` yields `T?` (`None` on empty slice); range indexes yield\n`Slice[T]`.",
              "source_path": "lang/std/memory/pointer.ks"
            },
            {
              "kind": "initializer",
              "name": "From Pointer",
              "anchor": "initializer-From-Pointer",
              "signature": "public init(pointer: Pointer[T], count: Int64)",
              "doc": "Builds a slice from an existing pointer and element count. The\ncaller is responsible for ensuring `count` elements live at `pointer`.",
              "source_path": "lang/std/memory/pointer.ks"
            },
            {
              "kind": "subscript",
              "name": "Indexed",
              "anchor": "subscript-Indexed",
              "signature": "public subscript[I](I) -> I.SliceYield { get set }",
              "doc": "Reads or writes at `index`, panicking on out-of-bounds.\n\nGeneric over `SliceIndex[T]`: `Int64` reads/writes a single\nelement, `Range[Int64]` and `ClosedRange[Int64]` read or replace\na sub-slice. Range writes require the source slice's length to\nmatch the range's length and panic otherwise. Sub-slices alias\nthe receiver's storage; don't outlive it.",
              "source_path": "lang/std/memory/pointer.ks"
            },
            {
              "kind": "subscript",
              "name": "Unchecked Index",
              "anchor": "subscript-Unchecked-Index",
              "signature": "public subscript[I](unchecked: I) -> I.SliceYield { get set }",
              "doc": "Reads or writes at `index` without a bounds check.\n\n# Safety\n\nUndefined behavior if the access falls outside `[0, count)`.",
              "source_path": "lang/std/memory/pointer.ks"
            },
            {
              "kind": "subscript",
              "name": "Wrapping",
              "anchor": "subscript-Wrapping",
              "signature": "public subscript[I](wrapped: I) -> I.SliceWrappedYield { get set }",
              "doc": "Reads or writes at `index` using modulo-wrapping. Yields `T?` so\nthe empty-slice case can surface as `None`.",
              "source_path": "lang/std/memory/pointer.ks"
            },
            {
              "kind": "field",
              "name": "count",
              "anchor": "field-count",
              "signature": "public var count: Int64 { get }",
              "doc": "Element count.",
              "source_path": "lang/std/memory/pointer.ks"
            },
            {
              "kind": "function",
              "name": "first",
              "anchor": "function-first",
              "signature": "public func first() -> Optional[T]",
              "doc": "First element, or `.None` for an empty slice.",
              "source_path": "lang/std/memory/pointer.ks"
            },
            {
              "kind": "field",
              "name": "isEmpty",
              "anchor": "field-isEmpty",
              "signature": "public var isEmpty: Bool { get }",
              "doc": "`true` when `count == 0`.",
              "source_path": "lang/std/memory/pointer.ks"
            },
            {
              "kind": "function",
              "name": "iter",
              "anchor": "function-iter",
              "signature": "public func iter() -> SliceIterator[T]",
              "doc": "Forward iterator over the elements.",
              "source_path": "lang/std/memory/pointer.ks"
            },
            {
              "kind": "function",
              "name": "last",
              "anchor": "function-last",
              "signature": "public func last() -> Optional[T]",
              "doc": "Last element, or `.None` for an empty slice.",
              "source_path": "lang/std/memory/pointer.ks"
            },
            {
              "kind": "field",
              "name": "pointer",
              "anchor": "field-pointer",
              "signature": "public var pointer: Pointer[T] { get }",
              "doc": "Pointer to the first element. `pointer.offset(by: i)` reaches\nelement `i` (0-indexed).",
              "source_path": "lang/std/memory/pointer.ks"
            }
          ]
        },
        {
          "kind": "protocol",
          "label": "Equatable",
          "source_path": "std.core.Equatable",
          "members": [
            {
              "kind": "function",
              "name": "equals",
              "anchor": "function-equals",
              "signature": "public func equals(Slice[T]) -> Bool",
              "doc": "Length-only equality (TODO: deepen to element-wise once an\niterator-based comparison is wired up). Not a true structural\nequality yet — handle with care in tests.",
              "source_path": "lang/std/memory/pointer.ks"
            }
          ]
        },
        {
          "kind": "protocol",
          "label": "ArrayMatchable",
          "source_path": "std.core.ArrayMatchable",
          "members": [
            {
              "kind": "typealias",
              "name": "Element",
              "anchor": "typealias-Element",
              "signature": "type Element = T",
              "doc": "",
              "source_path": "lang/std/memory/pointer.ks"
            },
            {
              "kind": "function",
              "name": "matchGet",
              "anchor": "function-matchGet",
              "signature": "public func matchGet(Int64) -> T",
              "doc": "Compiler-driven element read; safe to skip the bounds check\nbecause the matcher emits `index < matchLength()` first.",
              "source_path": "lang/std/memory/pointer.ks"
            },
            {
              "kind": "function",
              "name": "matchLength",
              "anchor": "function-matchLength",
              "signature": "public func matchLength() -> Int64",
              "doc": "Element count, exposed to the pattern matcher.",
              "source_path": "lang/std/memory/pointer.ks"
            },
            {
              "kind": "function",
              "name": "matchSlice",
              "anchor": "function-matchSlice",
              "signature": "public func matchSlice(Int64, Int64) -> Slice[T]",
              "doc": "Sub-slice for rest-pattern bindings (`..rest`). As above, the\nmatcher guarantees `0 <= from <= to <= matchLength()`.",
              "source_path": "lang/std/memory/pointer.ks"
            }
          ]
        }
      ]
    },
    {
      "kind": "struct",
      "name": "SliceIterator",
      "anchor": "struct-SliceIterator",
      "signature": "public struct SliceIterator[T] { /* private fields */ }",
      "doc": "Forward iterator over a `Slice[T]`. Holds a moving pointer and a\nremaining count; advancing reads through the pointer.\n\n# Representation\n\nA `Pointer[T]` cursor and an `Int64` countdown.",
      "source_path": "lang/std/memory/pointer.ks",
      "member_groups": [
        {
          "kind": "direct",
          "label": null,
          "source_path": null,
          "members": [
            {
              "kind": "initializer",
              "name": "From Storage",
              "anchor": "initializer-From-Storage",
              "signature": "public init(ptr: Pointer[T], remaining: Int64)",
              "doc": "Builds an iterator from a starting pointer and remaining count.",
              "source_path": "lang/std/memory/pointer.ks"
            }
          ]
        },
        {
          "kind": "protocol",
          "label": "Iterator",
          "source_path": "std.iter.Iterator",
          "members": [
            {
              "kind": "typealias",
              "name": "Item",
              "anchor": "typealias-Item",
              "signature": "type Item = T",
              "doc": "",
              "source_path": "lang/std/memory/pointer.ks"
            },
            {
              "kind": "function",
              "name": "next",
              "anchor": "function-next",
              "signature": "public mutating func next() -> Optional[T]",
              "doc": "Yields the next element, or `.None` when the count reaches zero.",
              "source_path": "lang/std/memory/pointer.ks"
            }
          ]
        }
      ]
    },
    {
      "kind": "struct",
      "name": "SystemAllocator",
      "anchor": "struct-SystemAllocator",
      "signature": "public struct SystemAllocator { /* private fields */ }",
      "doc": "`Allocator` backed by libc `malloc`/`free`/`realloc`. Used as the\ndefault `GlobalAllocator` and by every collection that doesn't pick a\ncustom allocator.\n\n# Memory Model\n\nStateless: the struct holds no fields. All bookkeeping lives in libc's\nheap. Cloning or copying the allocator has no effect on the heap state.",
      "source_path": "lang/std/memory/allocator.ks",
      "member_groups": [
        {
          "kind": "direct",
          "label": null,
          "source_path": null,
          "members": [
            {
              "kind": "initializer",
              "name": "Default",
              "anchor": "initializer-Default",
              "signature": "public init()",
              "doc": "Builds a stateless system allocator. No heap interaction occurs here.",
              "source_path": "lang/std/memory/allocator.ks"
            }
          ]
        },
        {
          "kind": "protocol",
          "label": "Allocator",
          "source_path": "std.memory.Allocator",
          "members": [
            {
              "kind": "function",
              "name": "allocate",
              "anchor": "function-allocate",
              "signature": "public mutating func allocate(Layout) -> RawPointer?",
              "doc": "Calls `malloc(layout.size)`. Alignment beyond `malloc`'s natural\nalignment (typically 16) is **not** honoured — types that need\nlarger alignment should use a different allocator.",
              "source_path": "lang/std/memory/allocator.ks"
            },
            {
              "kind": "function",
              "name": "deallocate",
              "anchor": "function-deallocate",
              "signature": "public mutating func deallocate(RawPointer, Layout)",
              "doc": "Calls `free(ptr)`. The `layout` argument is ignored — kept for\nprotocol conformance; allocators that need it (arenas) use it.",
              "source_path": "lang/std/memory/allocator.ks"
            },
            {
              "kind": "function",
              "name": "reallocate",
              "anchor": "function-reallocate",
              "signature": "public mutating func reallocate(RawPointer, Layout, Layout) -> RawPointer?",
              "doc": "Calls `realloc(ptr, newLayout.size)`. As with `allocate`, only\n`malloc`-natural alignment is guaranteed.",
              "source_path": "lang/std/memory/allocator.ks"
            }
          ]
        }
      ]
    }
  ]
}