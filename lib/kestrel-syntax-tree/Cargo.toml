[package]
name = "kestrel-syntax-tree"
version.workspace = true
edition.workspace = true

[lints]
workspace = true

[dependencies]
rowan = { workspace = true }
kestrel-span = { path = "../kestrel-span" }
kestrel-lexer = { path = "../kestrel-lexer" }
