# Stage 3 — Semantics

**Status: BLANK — needs exploration.** The core unexplored item: the
structural contains-a-reference predicate behind `Static`. A false-positive
`Static` answer silently re-opens dangling-storage holes stdlib-wide — the
same blast radius implicit-Copyable injection has historically had
(`references.md` §9).
